//Имитируем получение данных сервера\\
//№1 App.jsx
1. import dataPizzas
2. Пройтись по данным методом map и передать весь объект пропсом
3. Отрисовать компонент <PizzaBlock/>
4. Указывать key

//№2 PizzaBlock: получаем данные
1. {title,price,imageUrl,sizes,types} эти данные мы получаем
2. Создать состояние для sizes && types в состояние хранить (0)
2.1 typesProperty создать переменную и в ней хранить тип пицци ['тонкое', 'традиционное']
2.2 Воспользоватся методом map и пройтись по types
2.3 onClick колбек функция добавлять в состояние type новый index
2.4 className подсветить выбранный пункт написать условие если состояние type строго равно значению которое мы перебираем методом map тогда 'active' иначе ''
2.5 Отрисовать внутри li статичнный массив типов, передать значение из map как индекс выбранного элемента в []

3.1 Воспользоватся методом map и пройтись по sizes
2.3 onClick колбек функция добавлять в состояние size новый index
3.3 className подсветить выбранный пункт написать условие если состояние size строго равно значению которое мы перебираем методом map тогда 'active' иначе ''
3.4 Отрисовать внутри li значение