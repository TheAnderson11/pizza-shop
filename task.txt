//React-TypeScript\\
#Переписываем JS на TS
#Различия ts файла от tsx
1. ts-не понимает HTML разметку.
2. tsx-понимает HTML разметку.
"Важно: При рендере компонента может возникнуть ситуация,
когда элемент который вы пытаетесь получить или использовать,
может не существовать.Нужно писать логику что делать если значение
будет пустым undefined or null"
Примеры1: const myElementRef = useRef<HTMLDivElement>(null);
Передаём null потому что данные ссылки приходят не сразу.
Пример2: if(myElementRef) {тогда делаем этот код} иначе после 
ничего или ожидание загрузки страници ,компонента и тд.

//№1 index.js
1. Изменить файл с js на tsx. 
2. Пишем условие на наличие rootElement ,перенести root в const 
и пишем для него if.Даём странице время для загрузки

//№2 App.js
1. Изменить файл с js на tsx. 
2. 

//№3 FullPizza.jsx
1. FullPizza: дать тип FunctionComponent.
2. Изменить файл с js на tsx. 
3. Создаём тип для useState передаём объект и в него передаем поля 
4. В условном рендере компонента передать любой HTML тег и в нем уже отображать строчку.

//Используем: [
  Types
]