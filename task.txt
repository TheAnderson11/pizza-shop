//Saving_filtering_parameters_in_the_URL\\
#part 2

/\/\/\/\/\/\/\/\/\/\/\/\
//№1 Home.jsx
1. Перенести запрос на сервер в новую функцию и передовать её в тот же useEffect."const axiosPizzas"
2. Создать два состояния useRef: [isSearch, isMounted] оба false;
3. useEffect создающий queryParamsURL писать условие, если isMounted.current "true:не обязательно" 
3.1 первый рендер был, создавать параметры иначе , без else isMounted.current делать true.

4. useEffect делающий parse ,когда мы спарсили данные делать isSearch.current true ,условие не нужно так как
4.1 первый рендер был и мы создали строку query запроса.

5. useEffect запрос на сервер , Если !isSearch.current не false , тогда отрисовывать иначе, 
5.1 делать isSearch.current = false;
/\/\/\/\/\/\/\/\/\/\/\/\

/\/\/\/\/\/\/\/\/\/\/\/\
//№ Home.jsx*
1. Функции расположить в логической последовательности.
/\/\/\/\/\/\/\/\/\/\/\/\

##Helper
//Используем: [
  useRef,
]

#useRef:является встроенним инструментом, который позволяет создавать изменяемую ссылку на элемент DOM или любое другое
#значение в функциональном компоненте. Он обычно используется для задач, таких как доступ и управление элементами DOM,
#управление фокусом и хранение значений,[которые не должны вызывать повторное отрисовывание компонента при их изменении]